mod d1;

use d1::{constants, muts, shadowing, variables, data_types, tuples, arrays};

fn main() {
    variables();
    muts();
    constants();
    shadowing();
    data_types();
    tuples();
    arrays();
}
